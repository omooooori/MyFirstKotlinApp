<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.wings.websarva.kotlinapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!-- <activity android:name=".ListViewSample"></activity> -->
        <!-- <activity android:name=".BMICalculation"></activity> -->
        <!-- <activity android:name=".GuessMyNumber"></activity> -->
        <!-- <activity android:name=".CalculatorApp"></activity> -->
        <!-- <activity android:name=".DiceApp"></activity> -->
        <activity android:name=".ListViewSample">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>